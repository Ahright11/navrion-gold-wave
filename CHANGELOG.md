# Deploy log

Append a line every time you deploy. Format:

```
- **YYYY-MM-DD** — <who> — <target> — <one-line summary>
```

---

- **2026-05-05** — Manolis — **prod + preview** — SEO pass: og:image (1200x630), summary_large_image Twitter card, LocalBusiness JSON-LD with geo + opening hours + ISO/RightShip credentials, sitemap.xml + robots.txt sitemap pointer.
- **2026-05-05** — Manolis — **production** (`nvshipping.com`) — promoted current `main` to apex; brings live in line with preview (was frozen at Apr 21 build before this).
- **2026-05-05** — Manolis — repo housekeeping — synced full source state to GitHub (233 files), renamed repo `navrion-gold-wave` → `nvshipping`, added DEPLOY.md / COLLAB.md / CHANGELOG.md.
- **2026-04-21** — Manolis — production (`nvshipping.com`) — initial React cutover from WordPress, applied client feedback round (ISO cert, Services tab default, charterer list, contact bg).
